# 🐛 Visual Feedback Tool - Quick Start

## Що це?

Інструмент для звітування про баги прямо на сайті. Тестувальники та адміни можуть клікнути на будь-який елемент і залишити коментар.

## ⚡ Швидкий старт (3 хвилини)

### 1. Деплой Firebase Rules

```bash
./deploy-bug-tool.sh
```

або вручну:

```bash
firebase deploy --only firestore:rules
```

### 2. Додайте собі права адміна

1. Відкрийте [Firebase Console](https://console.firebase.google.com/)
2. Firestore Database → `users` → Знайдіть вашого користувача
3. Додайте поле: `isAdmin: true`
4. Збережіть

### 3. Перезавантажте сайт

- Очистіть кеш (Ctrl+Shift+R)
- ✅ Побачите плаваючу кнопку 🐛 в правому нижньому куті

## 🎯 Як використовувати

### Створити звіт про баг:

1. Натисніть кнопку 🐛
2. Клікніть на проблемний елемент
3. Опишіть проблему
4. Натисніть "Відправити"

### Переглянути звіти:

1. Перейдіть на `/admin/settings`
2. Прокрутіть до секції "Звіти про баги"
3. Натисніть "Переглянути на сайті" щоб побачити маркер

## 📊 Що включено

- ✅ Lazy Loading (0% впливу на клієнтів)
- ✅ Event Interception (перехоплення кліків)
- ✅ Visual Markers (червоні піни на сторінці)
- ✅ Admin Panel (управління звітами)
- ✅ Status Management (new/in_progress/resolved/rejected)
- ✅ Firebase Security Rules
- ✅ TypeScript Types
- ✅ Mobile Support

## 🔐 Performance

**Гарантовано:**
- Regular users: +0 KB, +0ms ✅
- Admins/Testers: +150 KB (lazy loaded)
- Zero network requests для клієнтів

## 📚 Документація

- **Повна документація:** [docs/BUG_REPORT_TOOL.md](docs/BUG_REPORT_TOOL.md)
- **Інструкції по налаштуванню:** [docs/BUG_REPORT_TOOL_SETUP.md](docs/BUG_REPORT_TOOL_SETUP.md)
- **Implementation Summary:** [BUG_REPORT_TOOL_SUMMARY.md](BUG_REPORT_TOOL_SUMMARY.md)

## 🛠️ Troubleshooting

**Кнопка не з'являється?**
- Перевірте `isAdmin: true` в Firestore
- Очистіть кеш браузера
- Перевірте Console на помилки

**Permission denied?**
```bash
firebase deploy --only firestore:rules
```

**Маркер не з'являється?**
- Перевірте URL: має бути `?bug_id=123`
- Перевірте Console на помилки

## 🎓 Для розробників

### Архітектура:

```
Components (Lazy Loaded)
    ↓
BugReportToolWrapper (checks role)
    ↓
BugReportTool (main logic)
    ↓
Firebase (bugReportService)
```

### Колекція Firestore:

```typescript
bug_reports/{reportId}
{
  url: string,
  xPos: number,      // % coordinates
  yPos: number,
  comment: string,
  status: string,    // new | in_progress | resolved | rejected
  userId: string,
  // ... metadata
}
```

### Security Rules:

- **read:** тільки isAdmin/isTester
- **create:** авторизовані з роллю
- **update/delete:** тільки isAdmin

## ✨ Готово!

Тепер ви можете:
- 🐛 Звітувати про баги прямо на сайті
- 🔍 Бачити точне місце проблеми
- 📊 Управляти статусами
- 🚀 Без впливу на клієнтів

---

**Version:** 1.0.0  
**Author:** GitHub Copilot  
**Date:** December 3, 2025
