# Руководство по совместной работе (Contributing Guide)

## 🤝 Добавление соавторов в репозиторий

### Для владельца репозитория

Чтобы дать соавтору возможность делать push в этот репозиторий, следуйте этим шагам:

#### 1. Добавление через веб-интерфейс GitHub:

1. Перейдите на страницу репозитория: https://github.com/DreamOdessa/DreamShopVo
2. Нажмите на **Settings** (Настройки) в верхнем меню
3. В левом меню выберите **Collaborators** (Соавторы) или **Collaborators and teams**
4. Нажмите кнопку **Add people** (Добавить людей)
5. Введите имя пользователя GitHub, email или полное имя соавтора
6. Выберите уровень доступа:
   - **Write** (Запись) - рекомендуется для соавторов (позволяет push)
   - **Maintain** (Поддержка) - расширенные права
   - **Admin** (Администратор) - полные права
7. Нажмите **Add [username] to this repository**

#### 2. Соавтор должен:

1. Проверить email - придет приглашение от GitHub
2. Принять приглашение, перейдя по ссылке в письме
3. После принятия приглашения, соавтор получит доступ к репозиторию

#### 3. Проверка доступа:

Соавтор может проверить доступ, выполнив:

```bash
git clone https://github.com/DreamOdessa/DreamShopVo.git
cd DreamShopVo
# Внести изменения
git add .
git commit -m "test: проверка доступа"
git push origin main
```

### Для соавтора

После получения приглашения:

1. **Клонирование репозитория:**
   ```bash
   git clone https://github.com/DreamOdessa/DreamShopVo.git
   cd DreamShopVo
   ```

2. **Настройка Git:**
   ```bash
   git config user.name "Ваше Имя"
   git config user.email "your.email@example.com"
   ```

3. **Установка зависимостей:**
   ```bash
   npm install
   ```

4. **Работа с изменениями:**
   ```bash
   # Создайте новую ветку для ваших изменений
   git checkout -b feature/my-feature
   
   # Внесите изменения
   # ...
   
   # Зафиксируйте изменения
   git add .
   git commit -m "feat: описание изменений"
   
   # Отправьте в удаленный репозиторий
   git push origin feature/my-feature
   ```

5. **Создайте Pull Request:**
   - Перейдите на GitHub
   - Нажмите кнопку "Compare & pull request"
   - Опишите изменения
   - Запросите review у владельца репозитория

## 📋 Правила работы

### Ветки (Branches)

- `main` - основная ветка, защищена от прямых push
- `feature/*` - для новых функций
- `fix/*` - для исправления багов
- `docs/*` - для изменений в документации

### Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: добавить страницу оплаты
fix: исправить отображение корзины
docs: обновить README
```

### Pull Requests

1. Создавайте отдельную ветку для каждой задачи
2. Убедитесь, что код работает и проходит все проверки
3. Опишите изменения в PR
4. Дождитесь review от владельца репозитория
5. Внесите правки, если потребуется
6. После одобрения PR будет смержен в main

### Перед коммитом

Убедитесь, что:

1. Код собирается без ошибок:
   ```bash
   npm run build
   ```

2. Проект запускается:
   ```bash
   npm start
   ```

3. Нет конфликтов с main веткой:
   ```bash
   git fetch origin
   git merge origin/main
   ```

## 🔒 Безопасность

- **Никогда не коммитьте:**
  - Firebase ключи и секреты
  - `.env` файлы с реальными данными
  - Пароли и токены
  - Личную информацию

- **Используйте `.env.local` для локальных настроек**

## 🛠️ Разработка

### Локальный запуск

```bash
npm install
npm start
```

### Сборка для продакшена

```bash
npm run build
```

### Структура проекта

```
src/
├── components/     # Переиспользуемые компоненты
├── pages/         # Страницы приложения
├── contexts/      # React контексты
├── firebase/      # Firebase конфигурация
└── types/         # TypeScript типы
```

## 📞 Контакты

Если возникли вопросы или проблемы:
- Создайте Issue на GitHub
- Свяжитесь с владельцем репозитория @DreamOdessa

## 📄 Лицензия

MIT License
