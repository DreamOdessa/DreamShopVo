# 🔥 Инструкция: Настройка Firebase Security Rules

## Проблема
Ошибка "Помилка завантаження даних з Firebase" возникает из-за неправильно настроенных правил безопасности Firestore.

## Решение

### Вариант 1: Через Firebase Console (Рекомендуется)

1. **Откройте Firebase Console:**
   - Перейдите на https://console.firebase.google.com/
   - Выберите проект `dreamshop-odessa`

2. **Откройте Firestore Database:**
   - В левом меню: `Build` → `Firestore Database`
   - Перейдите на вкладку `Rules`

3. **Замените правила:**
   - Удалите все текущие правила
   - Скопируйте содержимое файла `firestore.rules` из корня проекта
   - Вставьте в редактор правил
   - Нажмите **"Publish"** (Опубликовать)

4. **Проверьте:**
   - Обновите сайт (Ctrl+Shift+R)
   - Попробуйте авторизоваться

---

### Вариант 2: Через Firebase CLI

Если у вас установлен Firebase CLI:

```bash
# 1. Убедитесь, что вы в корне проекта
cd C:\Users\SenonKray\DreamShop

# 2. Залогиньтесь в Firebase (если ещё не залогинены)
firebase login

# 3. Деплой правил
firebase deploy --only firestore:rules
```

---

## Что делают новые правила?

### ✅ Для обычных пользователей:
- Могут **читать** все товары и категории
- Могут **создавать** свои заказы
- Могут **читать** только свои заказы
- Могут **редактировать** только свой профиль

### ✅ Для администраторов (isAdmin: true):
- Полный доступ к товарам, категориям и заказам
- Могут менять статусы заказов
- Могут редактировать профили пользователей

### ✅ Для неавторизованных:
- Могут только **читать** товары и категории
- Не могут создавать заказы или профили

---

## Временное решение для тестирования

Если нужен **полный публичный доступ** для теста (⚠️ небезопасно для продакшена):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

⚠️ **Не используйте это в продакшене!** Это открывает базу для всех.

---

## Проверка работы

После публикации правил:

1. Откройте DevTools (F12) → вкладка Console
2. Обновите страницу
3. Если видите ошибки Firebase — скопируйте их и отправьте мне

---

## Частые ошибки

### "Missing or insufficient permissions"
- Правила не опубликованы или неправильные
- Пользователь не авторизован (для заказов/профиля)

### "PERMISSION_DENIED"
- У пользователя нет прав админа (для админ-панели)
- Нужно добавить `isAdmin: true` в документ пользователя

---

## Как сделать пользователя админом

1. Откройте Firebase Console → Firestore Database → вкладка Data
2. Найдите коллекцию `users`
3. Найдите документ вашего пользователя (по email/uid)
4. Добавьте поле: `isAdmin` (boolean) = `true`
5. Сохраните

Теперь этот пользователь будет иметь полные права администратора.
