# 🚀 Швидкий деплой Bug Report Tool

## Крок 1: Деплой Firestore Rules

```bash
# В корені проєкту виконайте:
firebase deploy --only firestore:rules
```

**Що відбудеться:**
- Завантажаться нові правила з `firestore.rules`
- Додасться захист для колекції `bug_reports`
- Тільки адміни/тестувальники зможуть читати/створювати звіти

## Крок 2: Перевірка правил в Firebase Console

1. Відкрийте [Firebase Console](https://console.firebase.google.com/)
2. Виберіть ваш проєкт
3. Firestore Database → Rules
4. ✅ Переконайтесь, що з'явились правила для `bug_reports`

## Крок 3: Налаштування першого тестувальника

1. Увійдіть на сайт як користувач
2. Відкрийте Firebase Console → Firestore Database
3. Знайдіть вашого користувача в колекції `users`
4. Додайте поле: `isAdmin: true` (або `isTester: true`)
5. Перезавантажте сторінку сайту

**Результат:** З'явиться плаваюча кнопка 🐛 в правому нижньому куті

## Крок 4: Перший тест

### Створення звіту:
1. Натисніть кнопку 🐛
2. Клікніть по будь-якому елементу на сторінці
3. Введіть коментар: "Тестовий баг"
4. Натисніть "Відправити"
5. ✅ Побачите toast: "Звіт про баг відправлено!"

### Перегляд в адмін-панелі:
1. Перейдіть на `/admin/settings`
2. Прокрутіть вниз до "Звіти про баги"
3. ✅ Побачите ваш звіт з деталями

### Перегляд на сайті:
1. В адмін-панелі натисніть "Переглянути на сайті"
2. ✅ Відкриється сторінка з червоним маркером
3. Клікніть на маркер
4. ✅ Побачите popup з коментарем

## Крок 5: Перевірка Performance (КРИТИЧНО!)

### Тест для звичайного користувача:
1. Вийдіть з аккаунту (або відкрийте Incognito)
2. Відкрийте DevTools → Network
3. Перезавантажте сторінку
4. ✅ Переконайтесь, що НЕ завантажується chunk з назвою, схожою на `BugReportTool`

### Тест для адміна:
1. Увійдіть як адмін
2. DevTools → Network → очистіть
3. Перезавантажте
4. ✅ Побачите НОВИЙ chunk завантажується (lazy loaded)

## Troubleshooting

### Кнопка не з'являється:
- Перевірте `isAdmin: true` в Firestore → users → [ваш_user_id]
- Очистіть кеш браузера (Ctrl+Shift+R)
- Перевірте Console на помилки

### Помилка "Permission denied" при створенні звіту:
```bash
# Передеплойте правила:
firebase deploy --only firestore:rules
```

### Маркер не з'являється:
- Перевірте URL: має бути параметр `?bug_id=123`
- Перевірте Console: можлива помилка завантаження даних
- Перевірте Firestore: чи існує документ з таким ID

## Додаткові налаштування

### Додати тестувальників (не адмінів):
```javascript
// В Firestore → users → [user_id]
{
  // ...інші поля
  isTester: true  // Отримає доступ до інструменту, але не буде адміном
}
```

### Індекси (якщо потрібно):
Firestore автоматично створить індекси при першому запиті.
Якщо побачите помилку з посиланням на створення індексу - просто клікніть по ньому.

## Готово! 🎉

Тепер ваша команда може:
- 🐛 Звітувати про баги прямо на сайті
- 🔍 Бачити точне місце проблеми
- 📊 Управляти статусами в адмін-панелі
- 🚀 Без впливу на продуктивність для клієнтів

## Швидкі посилання

- 📖 Повна документація: `docs/BUG_REPORT_TOOL.md`
- 🔧 Firebase Console: https://console.firebase.google.com/
- 🛠️ Admin Settings: `https://ваш-домен.com/admin/settings`

---

**Питання?** Перегляньте `docs/BUG_REPORT_TOOL.md` для детальної інформації.
