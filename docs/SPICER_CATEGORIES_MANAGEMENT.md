# Управління категоріями Spícer (Spícer Category Management)

## Огляд

Додана нова функціональність для окремого управління категоріями алкогольних напоїв Spícer в адміністративній панелі. Це дозволяє:

✅ Створювати, редагувати та видаляти категорії специфічно для Spícer продуктів  
✅ Мати власну ієрархію категорій (батьківські та підкатегорії) окремо від DreamShop  
✅ Завантажувати зображення для кожної категорії  
✅ Фільтрувати Spícer товари по цих категоріях на сторінці `/spicer-products`  

## Архітектура

### Таблиця категорій у Firebase Firestore

Категорії зберігаються в колекції `categories` з наступною структурою:

```typescript
interface Category {
  id: string;
  name: string;                    // Назва категорії, наприклад "Крепкі алкоголі"
  slug: string;                    // URL-friendly identificator, наприклад "strong-alcohol"
  description: string;             // Опис категорії
  icon: string;                    // Emoji або символ, наприклад "🍾"
  image?: string;                  // URL зображення категорії
  isActive: boolean;               // Чи активна категорія
  sortOrder: number;               // Порядок сортування
  parentSlug?: string;             // Slug батьківської категорії
  // Для Spícer: parentSlug = 'spicer-root' для основних категорій
  //             parentSlug = '<parent-slug>' для підкатегорій
}
```

### Ідентифікація категорій Spícer

Категорії розділяються на:
- **DreamShop**: категорії без `parentSlug` або з `parentSlug` != 'spicer-*'
- **Spícer**: категорії з `parentSlug === 'spicer-root'` (основні) або `parentSlug` що починається з 'spicer-'

### Компоненти

#### 1. `SpicerCategoryManager.tsx`
Компонент для управління категоріями Spícer в адміністративній панелі.

**Функціональність:**
- Відображення основних категорій Spícer в сітці (grid layout)
- Показ підкатегорій для кожної основної категорії
- Додавання нових категорій з формою
- Редагування існуючих категорій
- Видалення категорій (з перевіркою на наявність привʼязаних товарів)
- Завантаження зображень в Firebase Storage
- Автоматичне генерування slug з назви (з підтримкою кирилиці)

**Стиль:** Помаранчева тема (gradient `#ff6b35` → `#ffa500`)

#### 2. `AdminPanel.tsx` (оновлено)
- Додана нова закладка `categoriesSubTab` для переключення між DreamShop та Spícer категоріями
- Два кнопки у вкладці "Категорії": 
  - 🛍️ Категорії DreamShop (синяя тема)
  - 🌶️ Категорії Spícer (помаранчева тема)

#### 3. `SpicerProductsPage.tsx` (оновлено)
- Інтеграція з контекстом AdminContext для отримання категорій Spícer
- Фільтрація товарів за категоріями з використанням нового поля `product.category`
- Динамічне популювання список категорій з бази даних

## Використання

### Створення категорії Spícer

1. Перейти в **Адміністративну панель** → **Категорії** → **🌶️ Категорії Spícer**
2. Клікнути кнопку **"Додати категорію"**
3. Заповнити форму:
   - **Назва**: наприклад "Крепкі алкоголі"
   - **Опис**: опис категорії
   - **Іконка**: emoji (наприклад 🍾)
   - **Зображення**: URL або завантажити з комп'ютера
   - **Батьківська категорія**: залишити "— основна категорія Spícer —" для основної категорії
4. Клікнути **"Додати"**

### Створення підкатегорії

1. На карточці основної категорії клікнути **"+ Підкатегорія"**
2. Вибрати основну категорію в полі "Батьківська категорія"
3. Заповнити інші поля та зберегти

### Привʼязання товарів до категорій Spícer

При створенні/редагуванні товару:

```typescript
{
  brand: 'spicer',           // або isSpicer: true
  category: 'strong-alcohol', // Slug категорії Spícer
  subcategory: 'vodka'       // (опціонально) Slug підкатегорії
}
```

## Фільтрація на сторінці Spícer

Сторінка `/spicer-products` тепер:

1. Завантажує категорії з Firestore (тільки з `parentSlug === 'spicer-root'`)
2. Відображає кнопки фільтрації за назвою категорії
3. Фільтрує товари за полем `product.category`

Приклад URL з фільтром:
```
/spicer-products?category=strong-alcohol
```

## Дані у Firebase

### Структура товару Spícer
```javascript
{
  id: "doc123",
  name: "Горилка преміум",
  brand: "spicer",
  isSpicer: true,
  category: "strong-alcohol",    // Slug категорії
  subcategory: "vodka",          // Slug підкатегорії (опціонально)
  price: 250,
  image: "https://...",
  volume: "0.5L",
  // ... інші поля
}
```

### Структура категорії Spícer
```javascript
{
  id: "cat123",
  name: "Крепкі алкоголі",
  slug: "strong-alcohol",
  description: "Складні спиртні напої...",
  icon: "🥃",
  image: "https://...",
  parentSlug: "spicer-root",    // Основна категорія
  isActive: true,
  sortOrder: 1
}
```

## Міграція даних

Якщо у вас вже є товари Spícer без правильної категорії:

1. **Вручну**: Відредагуйте кожен товар в адміністративній панелі
2. **Через скрипт**: Напишіть скрипт, що оновить поле `category` на основі старого значення

## Тестування

1. Створіть категорію: "Горілки" з slug `vodka` та parentSlug `spicer-root`
2. Створіть товар з `brand: "spicer"` та `category: "vodka"`
3. Перейдіть на `/spicer-products` і перевірте, що товар відображається
4. Фільтруйте по "Горілки" і переконайтеся, що товар показується

## Файли, що були змінені

- ✅ `src/components/SpicerCategoryManager.tsx` - НОВИЙ файл
- ✅ `src/pages/AdminPanel.tsx` - Додана закладка для Spícer категорій
- ✅ `src/pages/SpicerProductsPage.tsx` - Оновлено для використання категорій з Firebase
- ✅ `src/types/index.ts` - Вже мав интерфейс Category з `parentSlug`

## Стилізація

### Кольорова схема Spícer
- **Основний колір**: #ff6b35 (помаранчевий)
- **Градієнт**: `linear-gradient(135deg, #ff6b35 0%, #ff8c42 50%, #ffa500 100%)`
- **Фон карточок**: #fef9f3 (світло-помаранчевий)
- **Обідок карточок**: #ffe4cc

### Кольорова схема DreamShop
- **Основний колір**: #00acc1 (бірюзовий)
- **Градієнт**: `linear-gradient(135deg, #4dd0e1 0%, #26c6da 50%, #00acc1 100%)`

## Будущі покращення

- [ ] Добавити функцію "Автоматичного розкладу товарів" як у DreamShop (із eучними кодами)
- [ ] Дозволити завантажувати кілька зображень за категорію
- [ ] Добавити функцію перевпорядкування категорій drag-and-drop
- [ ] Синхронізувати категорії з 1С чи іншою системою

## Питання та підтримка

При виникненні проблем:

1. Перевірте, що товар має `brand: 'spicer'` або `isSpicer: true`
2. Перевірте, що категорія існує в Firestore з `parentSlug: 'spicer-root'`
3. Перевірте консоль браузера на помилки (F12 → Console)
4. Перевірте логи Firebase в адміністративній консолі

